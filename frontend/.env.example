# Leave unset for production same-origin requests through the reverse proxy.
# Set only when the frontend development server connects directly to FastAPI.
VITE_AUTHSTATUS_API_BASE_URL=http://localhost:8000